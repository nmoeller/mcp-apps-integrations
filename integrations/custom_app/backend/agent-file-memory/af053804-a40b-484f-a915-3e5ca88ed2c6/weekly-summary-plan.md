# Weekly Summary Plan

1. Retrieve the complete weekly task-completion dataset using the weekly-delivery-summary workflow.
2. Render bar, line, and pie charts from the returned records.
3. Report the top task completer and the team total, using only returned values and avoiding performance inferences.

Note: The skill instructions reference a `get_mock_data` tool, but that tool is not currently exposed in this session. If it remains unavailable, ask the user to provide the weekly records or enable the data tool.